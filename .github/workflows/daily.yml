name: TEST

on:
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest

    steps:
      - name: Debug
        run: |
          echo "URL=${{ secrets.XREA_URL }}"
          echo "SECRET=${{ secrets.XREA_SECRET }}"
