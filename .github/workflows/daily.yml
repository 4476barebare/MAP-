name: Daily XREA Trigger

on:
  workflow_dispatch:
  schedule:
    - cron: "0 0 * * *"

jobs:
  run:
    runs-on: ubuntu-latest

    steps:
      - name: Call PHP
        run: |
          echo "test"
